# Setup

The configured order is controlled by `demo/scribpy.toml`.

## Build Targets

Return to the [index](../index.md), then build Markdown, single-page HTML, and
the MkDocs-backed site with the commands from the demo README.
